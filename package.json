{
  "name": "privateroute-mini",
  "version": "1.0.0",
  "description": "Lightweight privacy proxy — zero dependencies, deploy to Render in seconds",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
